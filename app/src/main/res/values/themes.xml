<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      The reminder sheet draws over whatever is on screen, so the window itself
      must be see-through. The sheet paints its own scrim, hence
      backgroundDimEnabled=false — otherwise the dimming stacks and the
      background goes almost black.
    -->
    <style name="Theme.Pesky.Transparent" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:label="Pesky Reminders"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Opened by a tap on the notification body and by its Snooze action;
             notification actions cannot raise UI via a receiver on Android 12+.

             taskAffinity="" is load-bearing. The launch carries
             FLAG_ACTIVITY_NEW_TASK, and with the default affinity (the package
             name) that made Android reuse the app's *existing* task and bring it
             to the front — so tapping the notification hauled MainActivity up
             behind this translucent sheet, and finishing left the user sitting in
             the app rather than back where they were. An empty affinity gives
             this a task of its own, always. -->
        <activity
            android:name=".ReminderActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Pesky.Transparent" />

        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <!-- Exported so the system can deliver BOOT_COMPLETED. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>

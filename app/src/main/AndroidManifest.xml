<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      The exact-alarm permissions are a pair, and both halves are needed.

      USE_EXACT_ALARM (API 33+) is a normal permission: granted at install, not
      revocable, so canScheduleExactAlarms() is always true and ReminderScheduler
      needs no guard. That is the right shape for this app — a reminder the user
      can silently switch off in Settings is indistinguishable from a broken one.

      But it does not exist below API 33, and minSdk here is 26. Because the app
      targets >= 31, Android 12 and 12L (API 31-32) require one of the "Alarms &
      reminders" permissions or setAlarmClock throws SecurityException — so on
      exactly those two versions the app would install and then fail to schedule
      anything. SCHEDULE_EXACT_ALARM capped at 32 covers that window; Android 12
      pre-grants it, so there is still no runtime code to write.

      The cap matters: left uncapped it would list the app under Settings →
      Alarms & reminders on Android 13+ with a toggle that cannot change
      anything, since USE_EXACT_ALARM already decides the answer there.

      ManifestPermissionsTest pins both halves. Nothing else would catch their
      loss — the emulator is API 35 and every device from 13 up stays green.
    -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:label="Pesky Reminders"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Opened by a tap on the notification body and by its Snooze action;
             notification actions cannot raise UI via a receiver on Android 12+.

             taskAffinity="" is load-bearing. The launch carries
             FLAG_ACTIVITY_NEW_TASK, and with the default affinity (the package
             name) that made Android reuse the app's *existing* task and bring it
             to the front — so tapping the notification hauled MainActivity up
             behind this translucent sheet, and finishing left the user sitting in
             the app rather than back where they were. An empty affinity gives
             this a task of its own, always. -->
        <activity
            android:name=".ReminderActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Pesky.Transparent" />

        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <!-- Exported so the system can deliver BOOT_COMPLETED. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
